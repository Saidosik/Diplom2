# Obsolete Certbot/IP bootstrap template.
# Production uses deploy/nginx/default.conf.template and host-mounted certificates from /etc/ssl/said-diplom/.
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    return 301 https://said-diplom.ru$request_uri;
}
