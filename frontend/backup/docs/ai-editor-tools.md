# AI tools in editors

The AI assistant is embedded into content creation screens:

- question editor: quality checklist, duplicate detection, related sources, preliminary AI answer;
- publication editor: title/excerpt/tags suggestions, outline generation, source suggestions, source block insertion.

These tools use the Laravel backend endpoints under `/api/ai/*` through the BFF route `/api/laravel/*`.
